{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "opscart-watcher.serviceAccountName" . }}
  namespace: {{ include "opscart-watcher.namespace" . }}
  labels:
    {{- include "opscart-watcher.labels" . | nindent 4 }}
{{- end }}
